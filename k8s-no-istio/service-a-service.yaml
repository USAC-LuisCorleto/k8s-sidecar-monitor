apiVersion: v1
kind: Service
metadata:
  name: service-a
  namespace: no-istio
spec:
  selector:
    app: service-a
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
